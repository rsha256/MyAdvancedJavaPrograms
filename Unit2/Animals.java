public interface Animals
{
    // This is my interface (Animals) and it is a collection of the abstract methods age, weight, doTricks, and makeSound 
    void age();
    void weight();
    void doTricks();
    String makeSound();
}